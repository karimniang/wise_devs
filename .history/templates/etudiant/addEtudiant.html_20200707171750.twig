{% extends 'base.html.twig' %}



{% block body %}

    <h2 class="text-center">Hello Veuillez Ajouter vos étudiants✅</h2>
    <div class="col-sm-6">
        <div class="form-group">
        {{form_start(formEtud)}}
        {{form_widget(formEtud)}}

        {{form_end(formEtud)}}
        </div>
    </div>
    <div class="col-sm-6">
    <button type="button" name="" id="" class="btn btn-primary ">Boursier</button>
    <button type="button" name="" id="" class="btn btn-primary ">Non boursier</button>
        <div class="form-group">
            <div id="bourse">
                {{form_start(formBourse)}}
                {{form_widget(formBourse)}}
                {{form_end(formBourse)}}
            </div>
            <div id="nonBourse">
                {{form_start(formNonBourse)}}
                {{form_widget(formNonBourse)}}
                {{form_end(formNonBourse)}}
            </div>
        </div>
    </div>
    
{% endblock %}
{% block javascripts %}
<script src="JS/ajoutEtudiant.js"></script>
{% endblock %}